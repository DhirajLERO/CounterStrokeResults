# `public/data/`

Unzip `counterstroke_pages_data.zip` here (from the GPU harvest in the **code** repo).

You should see `index.json`, `metrics/`, `factual/`, `blank/`, and `evolution/` at this level.

This folder is what GitHub Pages serves; the site repo is separate from GracoImage.
